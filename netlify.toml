[build]
  command = "npm ci --include=dev && npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/api/anthropic/v1/messages"
  to = "/.netlify/functions/anthropic"
  status = 200
  force = true
